Submission Results-

This folder contains the evaluation evidence for the Transformer
reimplementation done by me:-

It includes files like-
1.training_log.txt : it contains the training and validation loss history.
2.bleu_results.txt : it contains latest  BLEU evaluation output.
3.Screenshot.png   : it conatins terminal screenshots captured during training/evaluation.

BLEU may be lower than the paper because this run uses a smaller dataset and a
limited training budget compared with the original WMT14 Transformer Base setup.
